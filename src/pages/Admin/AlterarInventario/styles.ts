import styled from 'styled-components'

export const InventarioList = styled.ul`
  display: grid;
  gap: 2rem;
  max-height: calc(100vh - 200px);
  overflow-y: auto;
  width: 75%;
  margin: 0 auto;
`
